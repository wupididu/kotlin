package com.example.note.db

class ListItem(id: Int,textTitle: String, textContent: String, textUri: String ) {

    var id = id
    var title = textTitle
    var content = textContent
    var uri = textUri

}